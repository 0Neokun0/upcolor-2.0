import { HomeLayout } from "components/templates"
import { Box } from "@mui/material"

const Home = () => {
    return (
        <Box>
            <HomeLayout />
        </Box>
    );
}

export default Home;
