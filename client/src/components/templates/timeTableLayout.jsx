import { Container, Grid } from "@mui/material"

const TimeTableLayout = (props) => {
    return (
        <Container>
            <Grid
                container
                sx={{
                    background: "gray",
                    height: "100%",
                }}
            >
                
            </Grid>
        </Container>
    )
}

export default TimeTableLayout